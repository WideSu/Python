#deque is a built in package from collections that can achieve queue without writting class.
from collections import deque

#methods:
append() #add an element to the right side of the deque (last one in)
appendleft() #add an element to the left side of the deque (becomes the first one)
clear() #remove all
count() #count number of occurences of target
pop() #remove and return the rightmost element
popleft() #remove and return the leftmost element
remove() #remove the first occurence of target
reverse() #reverse
